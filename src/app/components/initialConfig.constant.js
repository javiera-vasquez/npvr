export function initialConfig() {
  'ngInject';

  var service = {
    now: 'Cosmos',
    channel: 'natgeo',
    menu: ['History Channel', 'Mis grabaciones', 'Vod', 'Para ti']
  }

  return service;
}
